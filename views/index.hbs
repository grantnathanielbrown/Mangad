<h1 class="above"> Click on an image to view it in fullscreen.</h1>
{{#if artworks}}
<div class="artwork-container">
    {{#each artworks}}
    <a href="{{this.id}}">
        <img class="index-img" src="{{url}}">
    </a> {{/each}}
</div>
{{/if}}
